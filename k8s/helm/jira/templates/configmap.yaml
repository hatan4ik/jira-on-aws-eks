apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "jira.fullname" . }}-config
data:
  # Place custom Jira config files here if needed.
  README: "Custom Jira configuration can be mounted from this ConfigMap."
